# Third-party notices

The source snapshots in this directory are unmodified excerpts from the
following repositories at exact commits recorded in
`authorities.lock.json`:

- Grafana (`grafana/`)
- Papermark (`papermark/`)
- Directus (`directus/`)
- NocoDB (`nocodb/`)
- Google Gemini CLI (`gemini-cli/`)
- Shopify CLI (`shopify-cli/`)
- the historical TypeScript OpenAI Codex CLI (`codex-ts-cli/`)

Each directory contains the license file pinned from the same repository and
revision. NocoDB retains both its root and `nc-gui` package license because the
two texts describe different branch coverage. No upstream project supplied a
separate root NOTICE file for the selected snapshots.
